{
  "name": "penpot-plugin-starter-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate-fonts": "node scripts/generate-font-catalog.mjs",
    "predev": "npm run generate-fonts",
    "dev": "vite build --watch",
    "prebuild": "npm run generate-fonts",
    "build": "tsc && vite build"
  },
  "dependencies": {
    "@penpot/plugin-styles": "1.3.2",
    "@penpot/plugin-types": "^1.4.2"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.0.5",
    "vite-live-preview": "^0.3.2"
  }
}
